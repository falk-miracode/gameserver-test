{
  "name": "@gameserver-test/schemas",
  "version": "1.0.0",
  "description": "Shared Colyseus schemas for gameserver-test",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "clean": "rimraf dist",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@colyseus/schema": "^3.0.60"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.0.4"
  }
}
